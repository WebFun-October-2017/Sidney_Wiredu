function fancyArray(symbol) {
    var jNames = ["James", "Jill", "Jane", "Jack"];

    for (var i = 0; i < jNames.length; i++) {
        console.log(i + " " + symbol + " " + jNames[i]);
    }
}
fancyArray("->");
